#include "arguments.h"

void process_arguments(int argc, char **argv) {
    
    int index = 1;
    while(index < argc) {
        if(argv[index] == "--show-tokens") {
            
        }
        
    }
}
